import 'package:flutter/material.dart';

class RepositoriesImpl extends StatelessWidget {
  const RepositoriesImpl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}